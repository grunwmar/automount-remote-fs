BEFORE running install.sh

    1) Edit dev_mount_run.sh to set missing values for variables
        DEV_ADDRESS=
        DEV_USER=
        DEV_PORT=
        DEV_DIRECTORY=
        DEV_MOUNT_POINT=

    2) Edit mount-dev.service to fill your username
        USERNAME=


Then in the directory run `sudo bash install.sh`